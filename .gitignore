bin/
obj/
.vs/
*.user

# Generated output. The facts file is a release artifact, not source, and the
# built table is 10 MB of somebody else's text.
out/
*.ini
facts.json

# Your own purchase history. Personal, and no use to anyone else.
receipts.txt

# Install bookkeeping and displaced files live beside the binary.
installed.json
backup/

# Release output is built, not source.
release/
